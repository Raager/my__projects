version: '3.8'

services:
  crud:
    image: registration
    container_name: registration
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8000:80
    restart: always
    volumes:
      - ./:/var/www/html



